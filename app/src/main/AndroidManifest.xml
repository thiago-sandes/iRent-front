<?xml version="1.0" encoding="utf-8"?>
<manifest
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:dist="http://schemas.android.com/apk/distribution"
        xmlns:tools="http://schemas.android.com/tools"
        package="praticas.irent">

    <uses-permission android:name="android.permission.INTERNET"/>

    <dist:module dist:instant="true"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".filtros.MaisActivity">
        </activity>
        <activity android:name=".filtros.PrecoActivity">
        </activity>
        <activity android:name=".filtros.LocalizacaoActivity">
        </activity> <!-- FILTROS -->
        <activity
                android:name=".filtros.FiltrosActivity"
                android:label="Filtros"/> <!-- COMENTÁRIOS -->
        <activity
                android:name=".exibirOferta.ComentariosActivity"
                android:label="Comentários"/> <!-- TELA EXIBIR OFERTA -->
        <activity
                android:name=".exibirOferta.ExibirOfertaActivity"
                android:label="Oferta"/> <!-- TELA CADASTRO USUÁRIO -->
        <activity android:name=".TelaCadastro"/> <!-- TELA CADASTRO OFERTA -->
        <activity android:name=".CadastroOferta"/> <!-- TELA MAIN -->
        <activity
                android:name=".MainActivity"
                android:label="Inicio"/> <!-- TELA RECUPERAR SENHA -->
        <activity
                android:name=".RecuperarSenhaActivity"
                android:label=""/> <!-- TELA LOGIN -->
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>